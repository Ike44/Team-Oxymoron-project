/// @description The Platformer Room Controller

timer = 300; // Initialize timer
asecond = true; // Make sure the timer lasts a second
do_timer = false; // Operator to do the timer
active_shoot = false; // If trivia is being performed, default is false because the game does not start
kills = 0;



























