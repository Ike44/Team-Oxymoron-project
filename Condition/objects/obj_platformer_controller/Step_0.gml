/// @description 

if(timer <= 0)
{
	do_timer = false;
} // Check to see if the timer has reached zero

if (asecond) and (do_timer)
{
	asecond = false;
	alarm[0] = 60; 
} // Calls alarm 0(see description)





















